/**
 * Supabase database types.
 *
 * Regenerate from your project schema with the Supabase CLI:
 *   supabase gen types typescript --project-id <ref> > app/types/database.types.ts
 *
 * Until then this is an empty schema so `useSupabaseClient<Database>()` is typed.
 */
export type Database = Record<string, never>;
