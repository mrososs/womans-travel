# CHECKPOINT — دُرّة | موقع رحلات السيدات

نقطة مرجعية توضّح ما تم إنجازه وما تبقّى مقابل **متطلبات العميل**.

- **التاريخ:** 2026-07-03
- **الفرع الحالي:** `demo` · **فرع التطوير:** `dev`
- **آخر إنجاز:** نشر على Vercel + صفحة checkout (نموذج بصري فقط) + السلة والمفضلة عبر Supabase

> مفتاح الحالة: ✅ مكتمل · 🚧 جزئي / يحتاج ربط حقيقي · ⬜ لم يبدأ

---

## 1. ملخص سريع (Executive summary)

الأساس التقني والتصميم والمحتوى ثنائي اللغة جاهزون بمستوى عالٍ (تصميم فخم، RTL،
موبايل أولاً، محتوى في Supabase مع RLS). **لكن أهم المتطلبات التجارية للعميل لم
تُنفَّذ بعد:**

1. **الدفع الإلكتروني السعودي حقيقي** — موجود كنموذج بصري فقط، لا بوابة فعلية (mada / Apple Pay).
2. **لوحة التقارير الشهرية للمؤسسة** (عدد المشترين + نسب الربح) — **غير موجودة إطلاقًا**.
3. **صفحة الموظفون** — **غير موجودة**.
4. **صفحتا القروبات الداخلية / الخارجية المنفصلتان** — موجودتان كفلتر فقط، لا كصفحتين مستقلتين في القائمة.
5. **بيانات التواصل الحقيقية** (واتساب المدير + بريد المسؤول + السجل التجاري) — كلها قيم وهمية (placeholders).

---

## 2. متطلبات العميل مقابل الحالة

| # | المتطلب (كما طلبه العميل) | الحالة | ملاحظات |
|---|---|---|---|
| 1 | موقع مميز وفخم وجذاب سهل الاستخدام | ✅ | نظام تصميم دُرّة (ذهبي وردي/كحلي)، مكوّنات، GSAP |
| 2 | متجاوب وسهل من الجوال (mobile-first) | ✅ | RTL + breakpoints، تخطيطات جوال |
| 3 | "دوبامين" / تجربة ممتعة وجذابة | 🚧 | يوجد GSAP وحركات؛ يُنصح بلمسات micro-interactions إضافية |
| 4 | ربط الدفع الإلكتروني السعودي | ✅ | بوابة **Moyasar** حقيقية: نموذج مستضاف (mada/Visa/MC)، `create`/`callback` + webhook (Edge Function)، تحقّق من جهة الخادم، مفاتيح في Vault. مفاتيح live جاهزة للنشر (راجع `docs/payments-moyasar.md`) |
| 5 | ربط/عرض السجل التجاري | ⬜ | لا يوجد أي عرض للسجل التجاري أو توثيق "معروف" في الفوتر أو التواصل |
| 6 | تواصل مع المدير عبر واتساب | 🚧 | زر واتساب عائم موجود لكن الرقم **وهمي** `966500000000` |
| 7 | بريد إلكتروني خاص بالمسؤول | 🚧 | نموذج تواصل يعمل، لكن البريد المعروض **وهمي** `hello@durrah.travel` |
| 8 | تقارير شهرية للمؤسسة (عدد مشتري البكجات + نسب الربح) | ⬜ | **لا توجد لوحة تحكم/أدمن ولا تقارير على الإطلاق** — أكبر فجوة |
| 9 | الصفحة الرئيسية | ✅ | `pages/index.vue` |
| 10 | رحلات القروبات الداخلية (النسائية) | 🚧 | موجودة كـ **فلتر** (`local`) داخل الوجهات/البكجات؛ لا صفحة مستقلة في القائمة |
| 11 | رحلات القروبات الخارجية (النسائية) | 🚧 | موجودة كـ **فلتر** (`intl`)؛ لا صفحة مستقلة في القائمة |
| 12 | صفحة الموظفون | ⬜ | **غير موجودة** — لا صفحة ولا جدول قاعدة بيانات |
| 13 | صفحة تواصل معنا | ✅ | `pages/contact.vue` + `server/api/contact.post.ts` يكتب في Supabase |
| 14 | الأمان / الموقع محمي | 🚧 | RLS على كل الجداول ✅؛ لكن مزوّدو الدخول (OAuth) غير مفعّلين، ولا تحصين أمني/رؤوس حماية/فحص |

---

## 3. ما تم إنجازه فعليًا (تفصيل)

### الأساس والبنية ✅
- Nx monorepo (Nuxt 4 / Vue 3 / Tailwind v3)، مكتبات `shared-ui` / `shared-utils` / `supabase-client`.
- نظام تصميم دُرّة كامل (~20 مكوّن)، توكنز، خطوط عربية، GSAP.
- ثنائية اللغة ar/en (`@nuxtjs/i18n`)، RTL/LTR، تبديل اللغة.
- نشر على Vercel (`vercel.json`, `docs/deploy-vercel.md`).

### المحتوى والقاعدة ✅
- Supabase حي (مشروع `durrah`)، مخطط + RLS + بذور، أنواع TypeScript مولّدة.
- جداول محتوى عامة: categories, trips, packages, services, products, reviews, faqs.
- جداول إدخال عام: bookings, newsletter_subscribers, contact_messages.
- نقاط API: `bookings`, `newsletter`, `contact`, `trips` — تكتب فعليًا في القاعدة.

### الصفحات الموجودة ✅/🚧
- الرئيسية، الوجهات، البكجات (+تفاصيل `packages/[id]`)، الخدمات، المنتجات، تواصل، تفاصيل رحلة `trip/[id]`.
- الحساب: `account/{index,orders,wishlist}`، المصادقة: `auth/{login,register,callback}`.
- السلة `cart.vue` والمفضلة عبر Supabase + composables (`useCart`, `useWishlist`, `useAuth`, `useProfile`).
- الدفع `checkout.vue` → **نموذج بصري فقط**.

---

## 4. ما لم يُنجَز بعد — قائمة عمل مرتّبة بالأولوية

### أولوية عُليا (متطلبات تجارية أساسية للعميل)

- [x] **بوابة الدفع السعودية الحقيقية (Moyasar)** ✅
  - `CheckoutPayment.vue` بنموذج Moyasar المستضاف (mada / Visa / MC) بدل النموذج البصري.
  - `server/api/payments/create` (ينشئ order pending ويحسب المبلغ من الخادم +15% ضريبة).
  - `server/api/payments/callback` (تحقّق عبر Moyasar API → paid → تفريغ السلة) + `webhook` (Nitro) + **Edge Function `moyasar-webhook`** (service_role، مفاتيح من Vault).
  - المفاتيح في Supabase Vault + `.env`؛ مفاتيح **live** مخزّنة للنشر — راجع `docs/payments-moyasar.md`.
  - تبقّى: اختبار متصفّح شامل ببطاقة تجريبية + ضبط webhook في لوحة Moyasar + متغيّرات Vercel للـ live.

- [ ] **لوحة التقارير الشهرية للمؤسسة (Admin dashboard)**
  - صفحة/قسم أدمن محمي بدور (role) — يتطلب جدول أدوار + سياسات RLS للأدمن.
  - تقارير: عدد مشتري البكجات شهريًا، الإيرادات، **نسب الربح** (يتطلب تخزين التكلفة/الهامش لكل بكج).
  - رسوم بيانية + تصدير (PDF/Excel اختياري).
  - **قرار مطلوب:** كيف تُحسب نسبة الربح؟ (سعر البيع − التكلفة) لكل بكج → نحتاج حقل تكلفة.

- [ ] **صفحة الموظفون**
  - جدول `employees` في Supabase (اسم، مسمّى، صورة، نبذة) + قراءة عامة عبر RLS.
  - صفحة `pages/employees.vue` + بطاقة موظف في نظام التصميم + بند في القائمة.

- [ ] **فصل القروبات الداخلية/الخارجية كصفحتين مستقلتين في القائمة**
  - `pages/trips/internal.vue` و `pages/trips/external.vue` (أو مسارات مفلترة مسبقًا)، وإضافتهما للـ Navbar بالمسمّيات النسائية الصريحة.

- [ ] **بيانات التواصل والتوثيق الحقيقية**
  - رقم واتساب المدير الحقيقي في `WhatsappFab.vue`.
  - بريد المسؤول الحقيقي في `contact.vue`.
  - عرض **رقم السجل التجاري** وشعار "معروف"/التوثيق في الفوتر.

### أولوية متوسطة (لإكمال التجارة الإلكترونية)

- [ ] تفعيل مزوّدي الدخول في لوحة Supabase (Email + Google + Facebook) + redirect `/auth/callback`.
- [ ] تحويل composables السلة/المفضلة إلى متاجر Pinia + دمج سلة الضيف عند الدخول.
- [ ] صفحة تفاصيل منتج غنية (معرض صور، تبويبات، برنامج يومي، يشمل/لا يشمل).
- [ ] صفحة/تفاصيل الطلبات بعد الدفع.

### الأمان (المتطلب: "أهم شيء يكون الموقع محمي")

- [ ] تدقيق سياسات RLS (`get_advisors`) ومعالجة التحذيرات.
- [ ] رؤوس أمان (CSP, HSTS, X-Frame-Options) عبر Nuxt/Vercel.
- [ ] حماية النماذج من الإساءة (rate limiting / CAPTCHA على contact/newsletter/bookings).
- [ ] التأكد أن كل المفاتيح السرية (Moyasar secret, service keys) على الخادم فقط.
- [ ] دور أدمن + RLS صارم لأي بيانات تقارير/إيرادات.

---

## 5. قرارات مطلوبة من العميل (Open decisions)

1. **بوابة الدفع:** Moyasar (افتراضي) أم Tap/HyperPay؟
2. **حساب نسبة الربح:** ما مصدر التكلفة لكل بكج (لحساب الهامش في التقارير)؟
3. **الدفع للضيوف:** هل يُسمح بالشراء بدون تسجيل، أم حساب إلزامي؟
4. **بيانات فعلية:** رقم واتساب المدير، بريد المسؤول، رقم السجل التجاري، أسماء وصور الموظفين.
5. **من يرى التقارير:** المدير فقط أم أدوار متعددة؟

---

## 6. مراجع

- خطة المنصة التفصيلية: `docs/plan.md`
- النشر: `docs/deploy-vercel.md`
- المخطط والهجرات: `supabase/migrations/`
