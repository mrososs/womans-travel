/**
 * GSAP integration home.
 *
 * Placeholder — animation composables/helpers (e.g. `useGsapContext`,
 * scroll-triggered timelines) will live here. For now we simply re-export
 * `gsap` so consumers import it from a single shared entry point:
 *
 *   import { gsap } from '@org/shared-utils';
 */
export { gsap } from 'gsap';
